const menu=document.querySelector('.menu-btn');const nav=document.querySelector('.site-nav');if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}))}document.querySelectorAll('#year').forEach(el=>el.textContent=new Date().getFullYear());const form=document.querySelector('#contact-form');if(form){form.addEventListener('submit',async e=>{e.preventDefault();const note=document.querySelector('#form-note');if(!form.checkValidity()){form.reportValidity();return}const btn=form.querySelector('.submit-btn');const data=Object.fromEntries(new FormData(form).entries());data.smsConsent=form.querySelector('[name="smsConsent"]').checked;btn.disabled=true;note.style.color='';note.textContent='Sending…';try{const res=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const result=await res.json().catch(()=>({}));if(res.ok&&result.ok){note.textContent="Thanks! Your message has been sent — we'll be in touch soon.";note.style.color='#8af5ca';form.reset()}else{note.textContent=result.error||'Something went wrong sending your message. Please call or email us directly.';note.style.color='#ff8a8a'}}catch(err){note.textContent='Something went wrong sending your message. Please call or email us directly.';note.style.color='#ff8a8a'}finally{btn.disabled=false}})}
